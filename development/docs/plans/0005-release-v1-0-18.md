---
format_version: 1
id: PLAN-0005
status: completed
created: 2026-09-01
updated: 2026-09-03
---

# Publish and install version 1.0.18

## Goal

The current Scoville Code state is published as the immutable patch release
`v1.0.18` on `origin/main` and GitHub. Afterward, exactly this SemVer tag and
this GitHub release remain remote. The local Codex and Claude installations are
replaced from the verified release tag and fully match the published five-file
package.

## Non-goals

- No correction of the open SkillOpt behavioral finding in this release.
- No publication of the local SkillOpt Studio workspace.
- No change or publication of other Scoville Skills.
- No moving, overwriting, or recreating an existing tag.
- No claim that maintainability qualification passed.

## Work items

### W-001 Finalize release candidate v1.0.18

Status: done
Depends on: []
Blocked by: []
Decisions: []
Outcome: The complete repository state describes v1.0.18 correctly and passes all local release checks without overstating behavioral evidence.
Acceptance: Changelog, README, benchmark evidence, Plan profile, Skill package, JSON, local links, diff, and package comparison pass. The SkillOpt finding remains documented as not_qualified. The final commit content has been reviewed in full.
Steps:
1. Update the Unreleased entry and README status to v1.0.18 and the observed SkillOpt boundary.
2. Review the canonical Skill, JSON, Plan, links, and diff completely.
3. Ensure that Codex, Claude, and the canonical package contain the same current Skill content before publication.
Evidence: [Changelog identifies the 2026-09-01 maintainability release as v1.0.18, README and benchmark evidence preserve the not_qualified result and sealed Holdout boundary, Canonical Codex and Claude Skill packages pass validation and match across 5 files, Plan profile validation passed with 5 plans 9 work items and 3 decisions, Evaluation JSON parsed with 10 unique cases and qualification evidence parsed with outcome not_qualified, Markdown link inspection passed across 17 files with 9 local links, New public prose contains no em dash en dash or semicolon, Git diff check passed with only a line-ending warning]

### W-002 Publish commit, push, and GitHub release

Status: done
Depends on: [W-001]
Blocked by: []
Decisions: []
Outcome: `origin/main`, the new `v1.0.18` tag, and the GitHub release point to the same verified commit.
Acceptance: One complete commit is published on origin/main. A new immutable annotated tag v1.0.18 and the GitHub release of the same name exist at exactly that commit. Remote main, tag, release title, and release text are verified.
Steps:
1. Commit all intended repository files and review the commit.
2. Publish main without force-pushing and verify the remote commit.
3. Create and push the annotated v1.0.18 tag.
4. Create the GitHub release from the verified tag and verify it remotely.
Evidence: [Release commit 2573b821030548de79bcec1985ff8cc14014e54b contains 15 intended files and passed staged diff plus secret-pattern inspection, origin main matched release commit 2573b821030548de79bcec1985ff8cc14014e54b after push, New annotated tag v1.0.18 was created without retargeting an existing tag, Remote peeled tag v1.0.18 resolves to release commit 2573b821030548de79bcec1985ff8cc14014e54b, GitHub Release Scoville Code Anti-AI-Slop v1.0.18 is published as latest and is neither draft nor prerelease, Release body preserves the 3 of 4 Validation result rejected proposal and sealed Holdout boundary]

### W-003 Establish the single-tag state and local installations

Status: done
Depends on: [W-002]
Blocked by: []
Decisions: []
Outcome: GitHub retains only v1.0.18, and both local Skill installations are byte-identical copies of this release tag.
Acceptance: The new release is confirmed before any cleanup. All older GitHub releases and local and remote tags are removed. Exactly v1.0.18 remains remote. The exact tag package is staged without repository metadata. Codex and Claude receive verified backups and are replaced by the tag package. Both installations pass Skill and complete manifest validation.
Steps:
1. Delete the old GitHub release only after v1.0.18 is confirmed.
2. Remove old remote and local tags after exact target inspection.
3. Extract the v1.0.18 package from the tag into a temporary directory and validate it.
4. Back up the named Codex and Claude directories and replace them with the tag package.
5. Validate inventory, hashes, validators, remote state, and a clean repository state.
Evidence: [New v1.0.18 Release and peeled tag were verified before any cleanup, GitHub Release v1.0.17 was deleted after successor verification, Remote and local tag v1.0.17 were deleted after exact target-set inspection, GitHub remote and local state each retain exactly v1.0.18, Release tag package contains exactly 5 expected files and every archive file matches its Git blob, Staged release package passed canonical Skill validation, Existing Codex and Claude install directories were backed up before replacement, Codex and Claude installations each passed Skill validation and exact 5-file inventory comparison, Codex Claude and release package hashes match for every file, Release package tree SHA256 is 1a5d6608c06c627b130256ab9483e677c41524b1e2f62d29df8da6d0098bacbf]
